===[ Web ]===

* better handling for domain and subdomain. sometimes need only subdomain
* create conf directory under web/ and add container-specific conf files there
* configure less processes per apache than default, since we serve only one site
* separate web and drupal dirs

===[ Database ]===

* mysql local changes into /etc/mysql/conf.d/ as specified at end of my.cnf
* make: add option to run non-interactively to import database. (make batch?)
* postgres

===[ General ]===

* general git
* general cron
* general full tgz backup of whole stuff with: make backup_full TARGET=...
* general regular backups
* genaral limit number of backups
* general update
* general logrotate
* general docker composer
* general monitoring
* general benchmarking
